# Feature Planning

Refine the provided feature overview into a Cursor-native execution plan, following the workflow defined in @feature-planning.mdc.

## Instructions

The feature overview below has been pre-discussed and summarized externally. Your job is NOT to re-discuss the feature — it is to produce a concrete, codebase-aware plan ready for Agent execution.

Work through any ambiguities or architectural decision points before finalizing the plan. If a step requires a design choice not covered in the overview, surface it now — do not embed assumptions silently.

Produce the plan as a numbered list of steps. Each step must include:
- **What:** exactly what will be created or changed (specific file, class, or function)
- **Why:** how it connects to the feature goal
- **Verify:** how to confirm this step is complete (`direnv exec . <build or test command>`)

Do not begin writing any code. This is planning only.

---

## Feature Overview

[Paste your pre-discussed feature summary here]
